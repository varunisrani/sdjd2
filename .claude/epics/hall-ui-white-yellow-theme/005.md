---
name: Update App Layout and Pages
status: open
created: 2025-11-17T22:28:27Z
updated: 2025-11-17T22:28:27Z
github: [Will be updated when synced to GitHub]
depends_on: [001]
parallel: true
conflicts_with: []
---

# Task: Update App Layout and Pages

## Description

Review and update the root application layout and main page components to ensure yellow theme consistency across all routes and layouts. Verify there are no layout-specific blue color overrides, update meta theme colors for mobile browsers, and validate yellow theme consistency across route transitions and responsive breakpoints.

**Files:**
- `/home/user/sdjd2/app/layout.tsx`
- `/home/user/sdjd2/app/page.tsx`

This task ensures the yellow theme is consistent at the application shell level and handles any global styling that might override component-level yellow theme.

## Acceptance Criteria

- [ ] No layout-specific blue color overrides in layout.tsx
- [ ] Meta theme-color updated to yellow (#FFC107) for mobile browsers
- [ ] Favicon/app icons reviewed (update if blue-branded)
- [ ] Page background colors don't conflict with yellow theme
- [ ] Navigation elements (if present) use yellow for active states
- [ ] Loading states/skeletons use yellow accents (if applicable)
- [ ] Error boundaries/error states use appropriate colors (not blue)
- [ ] Yellow theme consistent across route transitions
- [ ] Responsive breakpoints (320px, 768px, 1024px, 1920px) all show yellow consistently

## Technical Details

**Files to Review:**
1. `/home/user/sdjd2/app/layout.tsx` - Root layout component
2. `/home/user/sdjd2/app/page.tsx` - Main page component

**Layout.tsx - Key Areas:**

1. **Meta Tags:**
   ```tsx
   // Update theme-color meta tag for mobile browsers
   <meta name="theme-color" content="#FFC107" />
   ```

2. **Global Styles:**
   - Check for any inline styles or className that override globals.css
   - Verify no blue background colors in root layout
   - Ensure body/html background supports yellow accents

3. **Navigation/Header/Footer:**
   - If navigation present, verify active link indicators use yellow
   - Check for blue hover states on nav links
   - Validate mobile menu toggle button uses yellow

4. **Loading States:**
   - Spinner or loading indicators should use yellow
   - Skeleton screens should have yellow accent hints

**Page.tsx - Key Areas:**

1. **Page-Level Components:**
   - Verify no page-specific blue color overrides
   - Check for blue accent sections or hero areas
   - Validate call-to-action buttons use yellow

2. **Layout Containers:**
   - Ensure container backgrounds don't conflict with yellow theme
   - Check for blue borders or dividers
   - Validate spacing/padding doesn't affect yellow visibility

3. **Responsive Behavior:**
   - Test yellow theme at all breakpoints defined in globals.css
   - Verify mobile drawer/modal overlays work with yellow
   - Check that yellow focus indicators are visible at all screen sizes

**Common Issues to Fix:**
```tsx
// BAD - Hardcoded blue theme-color
<meta name="theme-color" content="#2563eb" />

// GOOD - Yellow theme-color
<meta name="theme-color" content="#FFC107" />

// BAD - Blue navigation active state
<Link className={isActive ? 'text-blue-600 border-b-2 border-blue-600' : ''}>

// GOOD - Yellow navigation active state
<Link className={isActive ? 'text-primary border-b-2 border-primary' : ''}>
```

**Testing Checklist:**
- [ ] Mobile browser address bar shows yellow theme-color
- [ ] Navigation active states show yellow (if applicable)
- [ ] Page loads without blue flashes or transitions
- [ ] Route changes maintain yellow theme consistency
- [ ] 320px mobile - yellow theme visible and functional
- [ ] 768px tablet - yellow theme consistent
- [ ] 1024px desktop - yellow theme consistent
- [ ] 1920px+ large desktop - yellow theme scales properly

## Dependencies

- [x] Task 001: Update CSS Design Tokens (must complete first)
- [ ] CSS custom properties propagated from globals.css
- [ ] Component updates (tasks 002-004) for consistent testing

## Effort Estimate

- **Size:** S (Small)
- **Hours:** 1.5 hours
  - 30 minutes: Code review for layout and page files
  - 15 minutes: Update meta tags and fix any discovered issues
  - 30 minutes: Visual testing across route transitions
  - 15 minutes: Responsive testing at all breakpoints
- **Parallel:** true (Can run in parallel with tasks 002, 003, 004 after task 001 completes)

## Definition of Done

- [ ] Code review completed for layout.tsx and page.tsx - no hardcoded blue colors found
- [ ] Meta theme-color updated to yellow (#FFC107)
- [ ] Favicon checked (documented if blue-branded, updated if trivial)
- [ ] All fixes implemented (if any hardcoded colors discovered)
- [ ] Manual testing passed:
  - Route transitions maintain yellow theme
  - Navigation active states (if applicable)
  - Loading states (if applicable)
  - Error boundaries (if applicable)
- [ ] Responsive testing passed at all breakpoints:
  - 320px (mobile)
  - 768px (tablet)
  - 1024px (desktop)
  - 1920px (large desktop)
- [ ] Mobile browser theme-color verified on actual device or emulator
- [ ] Layout-specific changes committed (if any)
- [ ] Yellow theme consistency verified in browser DevTools
- [ ] No console errors or warnings related to color styles
