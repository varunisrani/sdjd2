---
name: Update CSS Design Tokens
status: open
created: 2025-11-17T22:28:27Z
updated: 2025-11-17T22:28:27Z
github: [Will be updated when synced to GitHub]
depends_on: []
parallel: false
conflicts_with: []
---

# Task: Update CSS Design Tokens

## Description

Replace blue primary colors with yellow palette in `globals.css`. This is the foundation task that updates the CSS custom properties (design tokens) to transform the application from blue to yellow theme. All subsequent component updates depend on these design token changes.

**Core Change:** Update CSS custom properties in `:root` selector to use Material Design Amber-based yellow palette while maintaining WCAG 2.1 AA accessibility compliance.

## Acceptance Criteria

- [ ] Updated `--primary` to `#FFC107` (Material Amber 500)
- [ ] Updated `--primary-dark` to `#FFA000` (darker yellow for hover/active states)
- [ ] Updated `--primary-light` to `#FFF9E6` (light yellow for backgrounds/highlights)
- [ ] Updated `--primary-hover` to `#FFD54F` (yellow for interactive feedback)
- [ ] Updated `--control-active` to `#FFC107` in music app specific variables
- [ ] Verified no hardcoded blue hex values remain in globals.css (#2563eb, #1e40af, #3b82f6)
- [ ] Documented color values and WCAG contrast ratios in code comments
- [ ] Created before/after screenshots for visual reference
- [ ] Verified automatic propagation to `.btn-primary`, `.music-btn`, `.progress-fill`, input focus states, and card hover states

## Technical Details

**File to Modify:** `/home/user/sdjd2/app/globals.css`

**Specific Changes:**

1. **Primary Color Variables (lines 5-9 approximately):**
   ```css
   :root {
     --primary: #FFC107;         /* Material Amber 500 - Yellow theme primary */
     --primary-dark: #FFA000;    /* Amber 700 - Hover/active states */
     --primary-light: #FFF9E6;   /* Light yellow - Backgrounds/highlights */
     --primary-hover: #FFD54F;   /* Amber 300 - Interactive feedback */
   }
   ```

2. **Music App Specific Variables (lines 36-40 approximately):**
   ```css
   --control-active: #FFC107;  /* Active control state */
   ```

3. **Verify Automatic Propagation:**
   - `.btn-primary` (line 179-187) - Should automatically use new yellow
   - `.music-btn` (line 320-337) - Should inherit yellow for active states
   - `.progress-fill` (line 357-361) - Should use yellow for progress bar
   - Input focus states (line 216-220) - Should show yellow focus rings
   - Card hover states (line 198-202) - Should use yellow accents

4. **WCAG Contrast Ratios:**
   - #FFC107 on white: 3.01:1 (meets WCAG AA for large text/UI components)
   - #FFA000 on white: 4.53:1 (meets WCAG AA for normal text)

**Implementation Approach:**
- Single-point update strategy: Change design tokens once, propagate automatically
- Keep existing secondary, neutral, and status colors unchanged
- Maintain current CSS architecture and class structure
- No component code changes required (CSS custom properties handle propagation)

**Edge Cases to Consider:**
- Ensure no blue color fallbacks are triggered
- Verify CSS custom property inheritance chain
- Check for any `!important` overrides that might block yellow theme

## Dependencies

- [x] CSS custom properties architecture (already exists in globals.css)
- [x] Design system foundation (commit 14484b6)
- [ ] Design approval of yellow palette (Material Amber 500 base)

## Effort Estimate

- **Size:** S (Small)
- **Hours:** 2 hours
  - 30 minutes: Update CSS custom properties
  - 30 minutes: Verify automatic propagation to existing classes
  - 30 minutes: Document changes and capture screenshots
  - 30 minutes: Manual testing across key components
- **Parallel:** false (This is the foundation task that blocks all component validation tasks)

## Definition of Done

- [ ] All CSS custom properties updated to yellow palette in globals.css
- [ ] No blue hex values (#2563eb, #1e40af, #3b82f6) remain in globals.css
- [ ] Before/after screenshots captured and documented
- [ ] WCAG contrast ratios documented in code comments
- [ ] Manual visual inspection confirms yellow appears on:
  - Primary buttons
  - Music player controls
  - Progress bars
  - Input focus rings
  - Card hover states
- [ ] Code committed with message: "Update design tokens to yellow theme"
- [ ] Changes pushed to feature branch
- [ ] Basic smoke test passed (application loads without CSS errors)
